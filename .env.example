# Required by the claude-code live test.
# Get one at https://console.anthropic.com/.
# The Agent SDK does not pick up the Claude Code CLI's logged-in
# session — only this env var.
ANTHROPIC_API_KEY=
